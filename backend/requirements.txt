# StudyOS Backend Dependencies
# Python 3.11+

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Configuration
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
chromadb==0.5.0

# AI / LLM
openai==1.30.1          # DeepSeek (OpenAI-compatible)
langchain==0.2.0        # RAG 编排
langchain-openai==0.1.8

# Embedding (本地免费)
sentence-transformers>=2.7.0

# Audio Processing (本地 Whisper)
openai-whisper>=20231117

# PDF Processing
pymupdf>=1.24.0        # PDF 文本提取 (自动选预编译 wheel)
pymupdf4llm>=0.0.4      # PDF → Markdown 结构化



# Export
pandoc==2.3             # Markdown → 多种格式
weasyprint==61.0        # HTML → PDF

# Utilities
httpx==0.27.0           # Async HTTP client
python-dotenv==1.0.1
tenacity==8.3.0         # Retry logic

# Dev
pytest==8.2.0
pytest-asyncio==0.23.7
ruff==0.4.4             # Lint + Format
